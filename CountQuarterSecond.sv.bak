module CountQuarterSecond(
	input logic clock50,
	input logic collision_has_changed,
	output logic freq_enable
);

logic [25:0] counter;
logic run_clock;

always_ff @(posedge clock50) begin 
	if (collision_has_changed)
		run_clock <= 1'b1;
end